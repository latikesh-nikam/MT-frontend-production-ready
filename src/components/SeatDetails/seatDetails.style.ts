import { styled } from '@mui/material/styles';
export const SeatDetailsContainer = styled('div')(
  ({ theme }: any) => `
   
  .cardContent{
    // height:10rem;
    // max-width:10rem;
  }
    `,
);
